import 'package:flutter/material.dart';

class musice extends StatefulWidget {
  musice({Key key}) : super(key: key);

  @override
  _musiceState createState() => _musiceState();
}

class _musiceState extends State<musice> {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("这是新增的内容"));
  }
}
